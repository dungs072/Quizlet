@using QuizletWebMvc.ViewModels.Terminology;
@using QuizletWebMvc.ViewModels.User;
@{
    Layout = null;
    int count = 1;
}
@model ListTermViewModel;

<!DOCTYPE html>
<html lang="en">
<head>
    <partial name="HeaderHome" />
</head>
<body>
    <div class="container-scroller">
        <!-- partial:../../partials/_sidebar.html -->
        <partial name="_Navigation" />
        <!-- partial -->
        <div class="container-fluid page-body-wrapper">
            <!-- partial:../../partials/_navbar.html -->
            <partial name="TopNavbar" />
            <!-- partial -->
            <div class="main-panel">
                <div class="content-wrapper">
                    <div class="row">
                        <div class="col-lg-12 grid-margin stretch-card">
                            <div class="card">
                                <div class="card-body">
                                    <h4 class="card-title">@Model.LearningModuleName</h4>
                                    <div class="template-demo">
                                        <a href="@Url.Action("FindRegisterClass", "Class", new{search = Model.GlobalSearch})" class="btn btn-outline-primary">Back</a>
                                        <a href="@Url.Action("RegisterClass", "Class", new{classId = Model.ClassId})" class="btn btn-outline-primary">Register</a>
                                    </div>
                                </div>
                                <div class="card-body">
                                    <h4 class="card-title">Flash cards</h4>
                                    <p class="card-description">

                                    </p>
                                    <div class="table-responsive">
                                        <table class="table table-dark">
                                            <thead>
                                                <tr>
                                                    <th> # </th>
                                                    <th> Flash card name </th>
                                                    <th> Explanation </th>
                                                </tr>
                                            </thead>
                                            <tbody>
                                                @foreach (var item in Model.Terms)
                                                {
                                                    <tr>
                                                        <td> @count </td>
                                                        <td>@item.TermName</td>
                                                        <td>@item.Explaination </td>
                                                    </tr>
                                                    count++;
                                                }
                                            </tbody>
                                        </table>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
                <!-- content-wrapper ends -->

            </div>
            <!-- main-panel ends -->
        </div>
        <!-- page-body-wrapper ends -->
    </div>
    <!-- container-scroller -->
    <partial name="FooterHome" />
    <partial name="Warnings" />
</body>
</html>
